// use const for creating array \\

const fruits = ["Apple", "Banana", "Mango"];

// we can add items in const array
fruits.push("Orange");

// we can change items in const array
fruits[0] = "Peach";

for (let i = 0; i < fruits.length; i++) console.log(fruits[i]);
